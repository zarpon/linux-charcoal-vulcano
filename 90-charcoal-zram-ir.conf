# Run after zram0 exists but before zram-generator assigns disksize.
# This makes the raw sysfs setup deterministic even on older generators that
# do not configure secondary recompression algorithms themselves.
[Service]
ExecStartPre=/usr/lib/charcoal/configure-zram-ir %I
